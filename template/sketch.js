// use ASSETS_PATH for shared assets, e.g.:
// let img;
// function preload() {
//   img = loadImage(ASSETS_PATH + 'my-image.jpg');
// }

function setup() {
  createCanvas(windowWidth - 32, windowHeight - 100);
}

function draw() {
  background(220);
}
